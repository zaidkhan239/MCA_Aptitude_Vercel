MCA Aptitude App (Vite + React + Tailwind)

Run locally:
1. npm install
2. npm run dev

Deploy to Vercel:
- Push repo to GitHub and import into Vercel. Build command: npm run build; Output dir: dist

The file public/questions.json is included in this project.
